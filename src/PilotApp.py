from App import App

#Let controller handle SSH, make models create the command
#Create class LogView
#Cannot move text cursor with keys
#Releasing one key stops the robot even if another key is pressed

myApp = App()